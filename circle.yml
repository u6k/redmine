machine:
    timezone: Asia/Tokyo
    services:
        - docker

test:
    override:
        - echo test

deployment:
    release:
        branch: master
        commands:
            - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
            # push redmine
            - docker pull redmine
            - docker tag redmine u6kapps/redmine:latest
            - docker push u6kapps/redmine
